# بوت إفادة على واتساب

بوت واتساب لمنصة إفادة التعليمية، يستخدم Baileys للاتصال وGroq للذكاء الاصطناعي.

## التقنيات
- **Node.js** + **Express**
- **@whiskeysockets/baileys** — اتصال واتساب
- **groq-sdk** — الذكاء الاصطناعي + تحويل الصوت
- **axios** — جلب بيانات الكورسات

## الملفات الرئيسية
- `index.js` — المنطق الكامل للبوت + السيرفر + API لوحة التحكم
- `public/dashboard.html` — لوحة التحكم (HTML/JS)
- `all_courses_details.json` — بيانات الكورسات (تُجلب تلقائيًا)
- `custom_knowledge.txt` — معلومات مخصصة للبوت (تُدار عبر لوحة التحكم)
- `lafsh_auth/` — بيانات مصادقة واتساب

## المنافذ
- **5000** — الخادم الرئيسي (لوحة التحكم + QR + API)

## الميزات
- بوت محادثة بالعربي (عبدالله — موظف سعودي حازم)
- دعم الرسائل الصوتية (Whisper STT)
- لوحة تحكم احترافية بالعربي
- إيقاف/تشغيل البوت لكل عميل
- مؤشرات: أخضر (بوت نشط) / أزرق (فريق الدعم يتابع)
- تصعيد تلقائي عند عجز البوت عن المساعدة
- إضافة معرفة مخصصة للبوت عبر لوحة التحكم
- عرض المحادثات مرتبة بالأحدث أولاً
